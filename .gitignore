# Old result directories
results_hpl*/
hpl_results*/

# Raw benchmark logs (large, keep locally only)
*.log
*.out
*.txt

# Keep only CSV in benchmark dirs
Dataset/arm_benchmark/*.xlsx
Dataset/x86_benchmark/*.xlsx

# ML experiments (keep locally, don't push)
ML Evaluation/
